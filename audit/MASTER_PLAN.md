# 🏗️ МАСТЕР-ПЛАН: Оптимизация и Укрепление

**Статус:** Черновик
**Дата:** 13.02.2026

На основе комплексного аудита, вот приоритетный план исправлений.

## 🔴 КРИТИЧНО (Блокеры для Релиза/Масштабирования)

1. **Исправить React Антипаттерн в `GoalsScreen.tsx`**
    - **Проблема:** `DeadlinePickerModal` определен внутри функции рендера.
    - **Влияние:** Серьезное падение производительности, баги с фокусом.
    - **Действие:** Вынести в отдельный файл компонента.

2. **Исправить N+1 Запросы на Бэкенде (Worker)**
    - **Проблема:** `POST /api/tasks` вставляет задачи по одной в цикле.
    - **Влияние:** Высокая задержка, нагрузка на БД.
    - **Действие:** Использовать `env.DB.batch()` или собрать один мульти-строчный `INSERT`.

3. **Устранить Ошибки Линтера**
    - **Действие:** Исправить обновления стейта и неиспользуемые переменные в `useTelegram.ts` / `useUser.ts`.

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ (Архитектура и Безопасность)

1. **Рефакторинг Монолитного Worker**
    - **Проблема:** Хендлер `worker.ts` слишком сложен и перегружен.
    - **Действие:** Внедрить роутер (например, `itty-router` или собственный простой роутер) и разделить на `routes/`.

2. **SQL Типизация**
    - **Проблема:** Сырые SQL-строки.
    - **Действие:** Внедрить легковесный Query Builder (например, `Kysely` или типизированные обертки) для предотвращения синтаксических ошибок и рисков инъекций.

3. **Безопасные Заголовки (CSP)**
    - **Действие:** Настроить конкретный Content-Security-Policy для защиты от XSS.

## 🟢 СРЕДНИЙ ПРИОРИТЕТ (Производительность и Полировка)

1. **Кеширование Фронтенда**
    - **Действие:** Внедрить `TanStack Query` (React Query) для замены ручного `useEffect` фетчинга и улучшения UX (мгновенные переходы).

2. **Оптимизация Бандла**
    - **Действие:** Провести аудит размера чанка `switch.js`.

---

## 🛠️ Порядок Выполнения

1. **Исправить Критичные:** (GoalsScreen, N+1, Lint) — *Оценка: 2 часа*
2. **Рефакторинг Worker:** (Роутер, Разделение файлов) — *Оценка: 3 часа*
3. **Кеширование Фронтенда:** (Миграция на React Query) — *Оценка: 4 часа*
